<div class="container">
  <div class="row">
    <div class="span6 offset3">
        <div class="well well large">
           <h2>Inscríbete</h2>
             <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => 'form-vertical' }) do |f| %>
             <%= f.error_notification %>
             <%= display_base_errors resource %>
             <%= f.input :name, :label => "Nombre",  :autofocus => true %>
             <%= f.input :email, :label => "Correo Electronico", :required => true %>
             <%= f.input :password, :label => "Contraseña", :required => true %>
             <%= f.input :password_confirmation, :label => "Confirma tu Constraseña",  :required => true %>
             <%= f.button :submit, 'Inscríbete', :class => 'btn-large btn-info' %>
           <% end %>
           <%= render "devise/shared/links" %>
        </div>
    </div>
  </div>
</div>  